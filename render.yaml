services:
  - type: web
    name: vision-forge-api
    runtime: python
    buildCommand: pip install -r requirements-web.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 1 --threads 2 --timeout 120 webapp:app
    healthCheckPath: /api/status